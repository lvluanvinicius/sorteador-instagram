import { Form } from "./form"; // Certifique-se de que o caminho para o componente está correto

export function Display() {
  return (
    <div className="h-screen">
      <Form />
    </div>
  );
}
